<%= form_for(@step_activity) do |f| %>
  <% if @step_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@step_activity.errors.count, "error") %> prohibited this step_activity from being saved:</h2>

      <ul>
      <% @step_activity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label : %><br />
    <%= f.text_field : %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
