<%= form_for(@tc, :url => project_test_case_path(@project, @tc)) do |f| %>
  <% if @tc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tc.errors.count, "error") %> prohibited this test case from being saved:</h2>

      <ul>
      <% @tc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
